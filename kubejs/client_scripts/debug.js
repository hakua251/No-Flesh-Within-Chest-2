NetworkEvents.dataReceived('test', event => {
    const player = event.player
    const level = event.level
    // let posX = Math.random() * 2 - 1
    // let posY = Math.random() * 2 - 1
    // let posZ = Math.random() * 2 - 1
    // posX = 0.75 * posX * Math.abs(posX) + 0.25 * posX
    // let distance = Math.sqrt(posX * posX + posY * posY + posZ * posZ)

    // posX = distance * posX
    // posY = distance * posY
    // posZ = distance * posZ
    // let size = 0.9
    // let star = $SpyglassAstronomyClient.addStar(new Vec3f(
    //     posX,
    //     posY,
    //     posZ
    // ),
    //     size,
    //     255,
    //     30,
    //     0,
    //     0.5,
    //     Math.random() * 2 - 1,
    //     Math.random() * 0.025 + 0.035
    // )
    // star.name = '测试2'
})